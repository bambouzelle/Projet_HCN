/*

    /!\ Fichier non utiliser /!\

*/
const {CommandInteraction} = require("discord.js")

/**
 * fonction d'éxemple
 * @param {CommandInteraction} interaction - interaction qui appelle la commande
 */
function command(interaction){
    /* code de ce que fait la commande */
}

module.exports.nomDeCommande = command; // exporter la commande pour que d'autre fichier (main.js) puisse l'utilisé